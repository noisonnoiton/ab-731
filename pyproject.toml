[project]
name = "ab-731"
version = "0.1.0"
description = "AB-731 (AI Transformation Leader) 강의 자료, 시험 대비 문서"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
docs = [
  "mkdocs-material>=9.5,<10",
  "plantuml-markdown>=3.10",
]

# Python 패키지로 배포하지 않고, 의존성/도구만 관리
[tool.uv]
package = false
index-url = "https://pypi.org/simple"
